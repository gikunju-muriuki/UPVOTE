name: Steem Auto-Vote Bot

on:
  schedule:
    # This instructs GitHub to run the script every 5 minutes
    - cron: '*/5 * * * *'
  workflow_dispatch: # Keeps manual execution enabled for testing

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Upvote Script
      env:
        STEEM_POSTING_KEY: ${{ secrets.STEEM_POSTING_KEY }}
      run: python bot.py

    - name: Save Voted History Log
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add voted_posts.txt || true
        git commit -m "Update voted posts log [skip ci]" || true
        git push || true
